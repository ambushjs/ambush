const {
    chunk,
    deduplicate,
    difference,
    flatten,
    intersection,
    merge,
    reverse,
    shuffle,
    sort,
} = require('../src');

const assert = require('assert/strict');

module.exports = function multiTests() {
    
}
